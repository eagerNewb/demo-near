export const Adapter = ({name}: any) => {
    switch(name) {
        case 'near': {
            // initiate and return connection to near network
        }
        case '' : {
        }
        default: {
            
        }
    }
}